{
  "name": "htttp-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "json-server": "json-server --watch server/db.json"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^4.4.5"
  }
}
